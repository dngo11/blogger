<div>
	<h3></h3>
	<% @article.comments.each do |c| %>
		<h4>Comment by <%=  c.author_name%></h4>
		<p class="comment"> <%= c.body %></p>

		<p>Posted <%= distance_of_time_in_words(Time.now, c.created_at) %> ago
		</p>

	<% end %>
</div>