<%= form_for(@article) do |f| %>
	
	<%= f.label :title %> 
	<%= f.text_field :title %>

	<%= f.label :body %>
	<%= f.text_area :body %>

	<%= f.submit %>


	<p>
		<%= f.label :tag_list %> <br />
		<%= f.text_field :tag_list %>
	</p>

<% end %>

